# Copy to .env (Python) and web/.env.local (Next.js), then fill in.

# Local Docker Postgres — port 5434, since 5432/5433 are already taken locally.
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/finance_rag

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
